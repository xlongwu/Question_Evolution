# 阶段06：流水线集成与验收回归

阶段编号：阶段06

阶段名称：流水线集成与验收回归

阶段目标：把新流程接入 `run_loop.sh`，形成可跑、可断点续跑、可验收的完整闭环。

阶段范围：更新编排顺序为评分、画像、分流、路由、进化、校验、候选选择、采答案、生成 rubric、评分、效果统计、状态更新。

主要任务：
- 保留 `collect_answers.py`、`gen_rubric.py`、`scoring.py` 当前逻辑。
- 调整 `run_loop.sh` 步骤和中间文件名。
- 补充 pytest。
- 跑 tiny sample 验证全链路。

涉及模块/文件范围：`run_loop.sh`、`tests/`、README 或阶段说明文档。

前置依赖：阶段01-05完成；人工确认实际输入文件路径和模型配置。

完成标准：小样本能从 `admitted_seed_samples.jsonl` 跑到 final scored，并生成 profile、route、validation、selection、effect、state、memory 产物。

测试与验证方式：mock API 单测；真实 tiny sample 冒烟测试；确认未修改 rubric prompt、score prompt 和扣分结构。

风险与注意事项：`run_loop.sh` 当前默认输入不是 `admitted_seed_samples.jsonl`，集成前必须确认替换策略；断点续跑不能重复写 memory。

预计交付物：集成后的循环脚本、测试集、冒烟测试记录、最终验收清单。
