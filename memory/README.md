# Memory Bank Skeleton

These files are reserved for future append-only JSONL artifacts:

- `operator_memory_bank.jsonl`
- `failure_memory_bank.jsonl`
- `invalid_generation_cases.jsonl`

Stage 1 defines the locations and schemas only. Automatic writes and memory
retrieval are Stage 5 responsibilities.
