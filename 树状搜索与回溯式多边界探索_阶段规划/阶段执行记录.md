# 阶段执行记录

## 2026-06-29 21:16:57 +08:00

### 自动选中的阶段

- 阶段编号：01
- 阶段文档：`阶段01_基础结构与搜索状态契约.md`

### 本阶段完成内容

1. 新增树状搜索状态契约 helper：提供旧单链 record 的默认补齐、稳定 root/current node ID、graph node 构造和 active frontier row 构造。
2. 扩展 `evolution_state` schema：加入 root/node/branch/boundary axis/depth/budget/discovered boundaries/frontier 推荐字段，并保留旧单链 stop status。
3. 新增 `search_graph.jsonl` 与 `active_frontier.jsonl` 的行级 schema。
4. 新增最小 JSONL 样例：覆盖根节点、分支节点、边界命中、重复分支、当前节点扩展、根节点 fork、父节点 fork。
5. 新增 focused pytest：验证旧单链记录可补齐、补齐后仍兼容现有 pipeline schema、graph/frontier 样例和构造行均通过 schema。
6. 新增树状搜索状态契约说明文档，明确字段含义、默认预算、兼容策略和后续阶段边界。

### 修改文件清单

- `schemas/evolution_state.schema.json`
- `schemas/README.md`

### 新增文件清单

- `search_state_contract.py`
- `schemas/search_graph_node.schema.json`
- `schemas/active_frontier.schema.json`
- `tests/test_tree_search_stage01_contract.py`
- `tests/fixtures/stage01_search_graph.jsonl`
- `tests/fixtures/stage01_active_frontier.jsonl`
- `docs/树状搜索状态契约.md`
- `树状搜索与回溯式多边界探索_阶段规划/阶段执行记录.md`

### 删除文件清单

- 无本阶段删除文件。

### 测试与验证结果

1. `python -m pytest tests/test_tree_search_stage01_contract.py tests/test_stage01_contract.py`
   - 结果：通过，11 passed。
2. `python -m py_compile search_state_contract.py schema_validation.py`
   - 结果：通过。
3. `python -m pytest`
   - 结果：未完全通过，39 passed、1 failed。
   - 失败项：`tests/test_stage06_run_loop_integration.py::test_run_loop_defaults_to_admitted_samples_with_legacy_fallback`。
   - 失败原因：当前 `run_loop.sh` 中 `DEFAULT_INPUT_FILE="data/data.jsonl"`，测试期望 `DEFAULT_INPUT_FILE="admitted_seed_samples.jsonl"`。该文件不属于本阶段改动范围，本阶段未顺手修改。

### 未完成事项

- 未将树状搜索契约接入 `profile_samples.py`、`select_evolution_candidates.py`、`operator_router.py`、`question_evolution.py`、`candidate_selection.py`、`analyze_evolution_effect.py`、`update_sample_state.py` 或 `run_loop.sh` 的真实流程。
- 未实现能力轴画像、动作决策、候选生成、边界去重、frontier 编排或预算扣减。
- 完整 pytest 中存在一个 Stage06 既有期望与 `run_loop.sh` 当前配置不一致的问题，需要在对应阶段或人工确认后处理。

### 风险与注意事项

- `ENABLE_TREE_SEARCH` 当前默认设为 `false`，用于保证旧单链流程仍是回退路径；后续是否默认开启需要人工或后续阶段确认。
- `node_id` 与 `dedup_signature` 采用稳定样本 ID / prompt hash 生成，后续并发写入与失败重跑的幂等性仍需阶段05验证。
- `search_graph.jsonl` 被定义为轨迹文件，不应在后续阶段替代主链输出。
- 当前工作区已有与本阶段无关的文件状态变化，未在本阶段处理。

### 是否满足当前阶段完成标准

- 是。阶段01要求的状态字段契约、graph/frontier 字段说明、旧单链兼容补齐、默认预算与最小样例均已落地，并通过 focused contract 验证。

### 是否建议进入下一阶段

- 是。建议下一次 Goal 进入阶段02。

### 下一次 Goal 自动应执行的阶段文档名称

- `阶段02_能力轴画像候选分流与路由决策.md`

## 2026-06-29 21:51:11 +08:00

### 自动选中的阶段

- 阶段编号：02
- 阶段文档：`阶段02_能力轴画像候选分流与路由决策.md`

### 本阶段完成内容

1. 在 `profile_samples.py` 中新增能力轴归一化、关键词映射和兜底推断逻辑，画像结果现在稳定包含 `boundary_axis_candidates`、`already_explored_axes`、`next_best_axes`。
2. 画像阶段会结合 `evolution_state.discovered_boundaries` 和阶段01状态契约，避免默认重复探索已命中或已结束的能力轴，并同步写回 `evolution_state.recommended_next_axes`。
3. 在 `select_evolution_candidates.py` 中保留旧 `evolution_action`，新增 `tree_search_decision`，支持 `expand_current_branch`、`fork_from_root`、`fork_from_parent`、`stop_branch`、`stop_sample` 五类动作。
4. 新增预算、分支状态、已探索能力轴、无可探索轴等分流规则；本阶段只输出建议，不扣减预算。
5. 在 `operator_router.py` 中显式消费能力轴、已探索轴、预算状态和 `tree_search_decision`，并扩展 `operator_route` 输出 `branch_intent`、`branch_action`、`source_node_type`、`target_boundary_axis`、`stop_reason` 等后续阶段所需字段。
6. 更新画像 prompt、可选 router prompt、schema 和契约说明，使阶段02新增字段进入本地字段契约。
7. 扩展 focused tests，覆盖 LLM 缺失新字段时的画像兜底、已探索能力轴不重复、root fork 路由和旧路由兼容。

### 修改文件清单

- `profile_samples.py`
- `select_evolution_candidates.py`
- `operator_router.py`
- `prompts/profile_prompt.py`
- `prompts/router_prompt.py`
- `schemas/sample_profile.schema.json`
- `schemas/pipeline_record.schema.json`
- `schemas/README.md`
- `tests/test_stage02_profile_candidates.py`
- `tests/test_stage03_operator_routing.py`
- `树状搜索与回溯式多边界探索_阶段规划/阶段执行记录.md`

### 新增文件清单

- 无本阶段新增文件。

### 删除文件清单

- 无本阶段删除文件。

### 测试与验证结果

1. `python -m py_compile profile_samples.py select_evolution_candidates.py operator_router.py prompts\profile_prompt.py prompts\router_prompt.py`
   - 结果：通过。
2. `python -m pytest tests/test_stage02_profile_candidates.py tests/test_stage03_operator_routing.py tests/test_tree_search_stage01_contract.py`
   - 结果：通过，13 passed。
3. `python -m pytest`
   - 结果：通过，42 passed。

### 未完成事项

- 未实现阶段03的 source node 候选生成、回溯生成模式或候选分支元数据写入。
- 未实现阶段04的边界识别、去重签名和边界叶子沉淀。
- 未实现阶段05的 `active_frontier.jsonl` 写入、`search_graph.jsonl` 追加、预算扣减或 `run_loop.sh` 集成。

### 风险与注意事项

- 能力轴映射目前是确定性关键词与画像字段兜底，真实 LLM 输出中若出现新表述，可能需要继续补充映射词。
- 本阶段新增的 `tree_search_decision` 和 router 扩展字段是建议层，不代表已经实际写入 frontier 或消费预算。
- 已探索能力轴默认不重复探索；如果后续需要“同轴更细分层”，阶段04或阶段05需要显式增加细分标记。
- 当前工作区在本阶段开始前已经存在阶段01相关未提交/未跟踪文件，本阶段未回退或清理这些既有状态。

### 是否满足当前阶段完成标准

- 是。每个待探索样本可得到能力轴候选和下一步动作；已命中的能力轴不会被默认重复探索；router 输出包含阶段03选择 source prompt 和 operator 所需的 source node、branch intent、operator 与 boundary axis；预算不足、无可探索轴、停止状态均有明确停止动作。

### 是否建议进入下一阶段

- 是。建议下一次 Goal 进入阶段03。

### 下一次 Goal 自动应执行的阶段文档名称

- `阶段03_回溯式候选生成与分支元数据.md`

## 2026-06-29 22:12:33 +08:00

### 自动选中的阶段

- 阶段编号：03
- 阶段文档：`阶段03_回溯式候选生成与分支元数据.md`

### 本阶段完成内容

1. 在 `question_evolution.py` 中新增 generation context 解析层，统一从 `_frontier_context`、顶层 frontier 字段、`tree_search_decision`、`operator_route` 和 `evolution_state` 中解析 `source_node_id`、`source_node_type`、`source_prompt`、`action_type`、`target_boundary_axis`、深度与预算字段。
2. 支持 `expand_current_branch`、`fork_from_root`、`fork_from_parent` 三种候选生成来源：当前节点使用当前 prompt，根节点 fork 可使用 `meta_info.prompt_old` 或 frontier `source_prompt`，父节点 fork 可使用 frontier `source_prompt` 或兼容性 parent prompt 字段。
3. 新增可选 CLI 参数 `--frontier-input`，允许将 `active_frontier.jsonl` 行按 `sample_id`、`index` 或 `search_root_id` 叠加到 `--input` 基础 pipeline record 上生成候选；该功能不接入 `run_loop.sh`，主流程集成留到阶段05。
4. 为候选输出写入分支元数据：`search_root_id`、`frontier_node_id`、`source_node_id`、`source_node_type`、`parent_node_id`、`generated_node_id`、`branch_id`、`boundary_axis`、`is_new_branch`、`search_depth`、`generation_action`、`prompt_source`、`source_prompt`。
5. 将上述元数据同步写入 `meta_info.question_evolution_metadata`、`candidate_generation` 和更新后的 `evolution_state`，使候选能追溯到 root/source/parent/branch。
6. 将 frontier id 纳入 `get_item_key` 与 `candidate_group_id`，避免同一样本多个 frontier 节点在断点续跑或多候选输出时发生冲突。
7. 在生成侧增加候选数量门控：本阶段只根据 `branch_budget_remaining`、`sample_budget_remaining` 和 `MAX_SAMPLE_BRANCHES` 压低生成数量，不扣减预算，预算扣减仍留给阶段05。
8. 扩展 `question_evolution_metadata.schema.json` 与 `schemas/README.md`，记录阶段03新增 source-node 与 branch metadata 字段。
9. 新增 focused pytest，覆盖当前分支扩展、根节点 fork、父节点 fork、frontier 叠加与 key 去重、预算压低候选数。

### 修改文件清单

- `question_evolution.py`
- `schemas/question_evolution_metadata.schema.json`
- `schemas/README.md`
- `树状搜索与回溯式多边界探索_阶段规划/阶段执行记录.md`

### 新增文件清单

- `tests/test_stage03_frontier_generation.py`

### 删除文件清单

- 无本阶段删除文件。

### 测试与验证结果

1. `python -m py_compile question_evolution.py`
   - 结果：通过。
2. `python -m pytest tests/test_stage03_frontier_generation.py`
   - 结果：通过，4 passed。
3. `python -m pytest tests/test_stage03_operator_routing.py tests/test_stage04_complexity_selection.py tests/test_tree_search_stage01_contract.py tests/test_stage02_profile_candidates.py`
   - 结果：通过，19 passed。
4. `python -m py_compile question_evolution.py search_state_contract.py profile_samples.py select_evolution_candidates.py operator_router.py`
   - 结果：通过。
5. `python -m pytest`
   - 结果：通过，46 passed。
6. `python question_evolution.py --help`
   - 结果：通过，CLI 帮助中已出现 `--frontier-input`。

### 未完成事项

- 未实现阶段04的边界识别、去重签名、候选选择扩展和边界叶子沉淀。
- 未实现阶段05的预算扣减、`active_frontier.jsonl` 写入、`search_graph.jsonl` 追加或 `run_loop.sh` 树状搜索集成。
- 未对真实 LLM/API 进行端到端调用；本阶段验证使用 mock client 与结构性 pytest。

### 风险与注意事项

- `--frontier-input` 需要基础 `--input` 中存在匹配 record，因为 active frontier 行本身不包含参考答案、rubric 和候选答案等生成 prompt 所需上下文。
- 父节点 fork 如果没有 frontier `source_prompt` 或显式 parent prompt 字段，会按兼容策略回退到 root prompt；真实父节点 prompt 的完整维护仍依赖阶段05的 graph/frontier 编排。
- 本阶段只限制生成数量，不扣减预算；如果后续阶段05没有集中扣减，可能出现预算状态只读不更新的问题。
- 当前工作区在本阶段开始前已存在阶段01/02相关未提交和未跟踪文件，本阶段未回退或清理这些既有状态。

### 是否满足当前阶段完成标准

- 是。给定同一样本的多个 frontier 节点，当前实现可以按 current/root/parent source node 生成候选；每个候选都能追溯到 `search_root_id`、`source_node_id`、`parent_node_id` 和 `branch_id`；根节点 fork、父节点 fork、当前分支扩展均有明确行为；候选数量受生成侧预算约束；旧单链输入仍可按默认当前节点生成候选，完整 pytest 已通过。

### 是否建议进入下一阶段

- 是。建议下一次 Goal 进入阶段04。

### 下一次 Goal 自动应执行的阶段文档名称

- `阶段04_边界识别候选选择与去重沉淀.md`

## 2026-06-29 22:28:36 +08:00

### 自动选中的阶段

- 阶段编号：04
- 阶段文档：`阶段04_边界识别候选选择与去重沉淀.md`

### 本阶段完成内容

1. 在 `analyze_evolution_effect.py` 中新增能力边界轴识别字段：`target_boundary_axis`、`boundary_axis_detected`、`boundary_axis_mismatch`、`boundary_axis_audit_reason`。
2. 新增新边界判定逻辑：结合 `effect_label`、分数变化、复杂度校验、重复题型风险、`expected_evaluation_focus` 与候选答案对齐情况，输出 `is_new_boundary_for_sample`、`duplicate_boundary_for_sample`、`structural_boundary_signal`、`boundary_candidate_status`。
3. 新增稳定 `dedup_signature` 规则：按样本、检测能力轴、期望失败/评价 focus/分支信息生成签名，并可与 `evolution_state.discovered_boundaries` 中已有签名或同轴历史边界对比，识别同一样本内重复边界。
4. 在 `candidate_selection.py` 中扩展边界选择标记：`selected_into_mainline`、`selected_as_boundary_leaf`、`discard_as_duplicate`、`dedup_signature`；当输入已包含 `effect_analysis` 时，会排除重复边界候选，并保留新边界叶子。
5. 保持旧流程兼容：无 `effect_analysis` 的预评分候选选择仍按原复杂度校验和综合分选择，不提前执行阶段05的 frontier 或预算逻辑。
6. 更新 `candidate_selection` 与 `effect_analysis` schema，并在 `schemas/README.md` 中记录阶段04字段归属。
7. 新增 focused pytest，覆盖新边界、同轴重复、目标轴与检测轴不一致、结构性信号无降分、重复候选排除与边界叶子保留。

### 修改文件清单

- `analyze_evolution_effect.py`
- `candidate_selection.py`
- `schemas/README.md`
- `schemas/candidate_selection.schema.json`
- `schemas/effect_analysis.schema.json`
- `树状搜索与回溯式多边界探索_阶段规划/阶段执行记录.md`

### 新增文件清单

- `tests/test_stage04_boundary_selection.py`

### 删除文件清单

- 无本阶段删除文件。

### 测试与验证结果

1. `python -m py_compile analyze_evolution_effect.py candidate_selection.py`
   - 结果：通过。
2. `python -m pytest tests/test_stage04_boundary_selection.py tests/test_stage04_complexity_selection.py`
   - 结果：通过，10 passed。
3. `python -m pytest tests/test_stage03_frontier_generation.py tests/test_stage05_effect_state.py tests/test_stage06_tiny_pipeline_artifacts.py`
   - 结果：通过，10 passed。
4. `python -m pytest tests/test_stage04_boundary_selection.py tests/test_stage04_complexity_selection.py tests/test_stage05_effect_state.py`
   - 结果：通过，15 passed。
5. `python -m pytest`
   - 结果：通过，50 passed。
6. `python analyze_evolution_effect.py --before tests/fixtures/stage05_previous_scored.jsonl --input tests/fixtures/stage05_scored.jsonl --output "$env:TEMP\qe_stage04_effect.jsonl" --matrix-output "$env:TEMP\qe_stage04_matrix.jsonl"`
   - 结果：通过，CLI dry-run 正常退出，并在临时输出中确认新增 `boundary_axis_detected`、`dedup_signature`、`is_new_boundary_for_sample` 等字段。
7. `python candidate_selection.py --input tests/fixtures/stage04_candidates_input.jsonl --output "$env:TEMP\qe_stage04_selected.jsonl" --no-invalid-output`
   - 结果：通过，CLI dry-run 正常退出，并在临时输出中确认新增选择标记字段。

### 未完成事项

- 未实现阶段05职责：未扣减 `sample_budget_remaining` / `branch_budget_remaining`，未生成下一轮 `active_frontier.jsonl`，未追加 `search_graph.jsonl`，未接入 `run_loop.sh` 的 frontier 消费。
- 未改变 `update_sample_state.py` 的停止编排逻辑；阶段04只提供边界判断与选择字段，供阶段05统一消费。
- 未对真实 LLM/API 进行端到端调用；验证使用 fixture、mock 和本地 CLI dry-run。

### 风险与注意事项

- `boundary_axis_detected` 采用结构化字段优先、关键词与算子映射兜底的确定性规则；真实 LLM 输出若出现新表述，可能需要继续补充关键词。
- 对“无降分但候选答案呈现结构性错误”的情况，本阶段只标记 `structural_boundary_signal` 与人工复核状态，不修改既有 `effect_label`，避免提前改变阶段05旧状态更新行为。
- `candidate_selection.py` 在当前主流程中仍位于评分之前，因此常规 run_loop 里多数候选不会提前带有 `effect_analysis`；带效果分析的边界叶子/重复排除能力主要供后续阶段05或离线复盘复用。
- 当前工作区在本阶段开始前已存在阶段01-03及其他无关的未提交/未跟踪变更，本阶段未回退或清理这些既有状态。

### 是否满足当前阶段完成标准

- 是。同一样本可通过 `is_new_boundary_for_sample` 沉淀不同能力轴边界；重复边界可通过 `duplicate_boundary_for_sample` 与 `discard_as_duplicate` 标记并在候选选择中排除；有效边界可通过 `selected_as_boundary_leaf` 保留；目标轴与检测轴不一致、证据不足和结构性信号均有审计字段；输出足以供阶段05更新 `discovered_boundaries`、branch 状态和 search graph。

### 是否建议进入下一阶段

- 是。建议下一次 Goal 进入阶段05。

### 下一次 Goal 自动应执行的阶段文档名称

- `阶段05_状态更新frontier编排与主流程集成.md`

## 2026-06-29 22:43:56 +08:00

### 自动选中的阶段

- 阶段编号：05
- 阶段文档：`阶段05_状态更新frontier编排与主流程集成.md`

### 本阶段完成内容

1. 在 `update_sample_state.py` 中新增树状搜索配置读取与 CLI 覆盖参数，支持 `ENABLE_TREE_SEARCH`、单样本预算、最大深度、最大边界数、无新增边界停止轮数、root fork 与 parent backtrack 开关。
2. 保持旧 `update_records()` 返回契约兼容，同时新增 `update_records_with_artifacts()`，统一产出更新后的主记录、memory entries、`search_graph.jsonl` 行和下一轮 `active_frontier.jsonl` 行。
3. 在状态更新阶段集中扣减 `sample_budget_remaining` 与 `branch_budget_remaining`，并根据阶段04的 `effect_analysis` 更新 `discovered_boundaries`、`already_explored_axes`、`recommended_next_axes`、`no_new_boundary_rounds`、`branch_status` 与样本级停止状态。
4. 实现分支停止与续探策略：有效边界命中、重复边界、无效复杂度、深度/预算耗尽会停止当前分支；若样本仍有预算，则按配置生成 root fork 或 parent fork；探索中且预算未耗尽的分支继续 `expand_current_branch`。
5. 实现 `search_graph.jsonl` 追加/合并逻辑，按样本、root、node 和 dedup signature 去重，记录父子关系、能力轴、分支状态、边界命中、主线/边界叶子/重复标记。
6. 实现 `active_frontier.jsonl` 生成逻辑，root/parent fork 不显式继承旧 `branch_id`，避免后续阶段把新分支误归入旧分支；禁用 root fork 和 branch backtrack 时可退回接近单链的行为。
7. 在 `run_loop.sh` 中新增树状搜索配置项与摘要输出；启用树状搜索时，下一轮 `question_evolution.py` 会消费上一轮 `active_frontier.jsonl`，`update_sample_state.py` 会写出本轮 `search_graph.jsonl` 和下一轮 `active_frontier.jsonl`，并在 frontier 为空时停止树状搜索循环。
8. 更新 `schemas/README.md`，明确阶段05由 `update_sample_state.py` 拥有预算扣减、状态更新和 graph/frontier artifact 生成。
9. 扩展 focused tests，覆盖边界命中后 root fork、深层终止分支 parent fork、预算耗尽停止样本、invalid 分支不继续扩展、禁用 fork 配置回退，以及 run loop 的 frontier/artifact 接入。

### 修改文件清单

- `update_sample_state.py`
- `run_loop.sh`
- `schemas/README.md`
- `tests/test_stage05_effect_state.py`
- `tests/test_stage06_run_loop_integration.py`
- `树状搜索与回溯式多边界探索_阶段规划/阶段执行记录.md`

### 新增文件清单

- 无本阶段新增文件。

### 删除文件清单

- 无本阶段删除文件。

### 测试与验证结果

1. `python -m pytest`
   - 结果：通过，56 passed。
2. `python -m py_compile update_sample_state.py analyze_evolution_effect.py candidate_selection.py question_evolution.py select_evolution_candidates.py operator_router.py`
   - 结果：通过。
3. `python -m pytest tests/test_stage05_effect_state.py tests/test_stage06_run_loop_integration.py`
   - 结果：通过，16 passed。
4. `python -m pytest tests/test_stage06_run_loop_integration.py`
   - 结果：通过，6 passed。
5. `python analyze_evolution_effect.py --before tests/fixtures/stage05_previous_scored.jsonl --input tests/fixtures/stage05_scored.jsonl --output %TEMP%/qe_stage05_effect_analysis.jsonl --matrix-output %TEMP%/qe_stage05_effect_matrix.jsonl`，随后执行 `python update_sample_state.py --input %TEMP%/qe_stage05_effect_analysis.jsonl --output %TEMP%/qe_stage05_state_updated.jsonl --tree-search-enabled true --active-frontier-output %TEMP%/qe_stage05_active_frontier.jsonl --search-graph-output %TEMP%/qe_stage05_search_graph.jsonl --no-memory-output`
   - 结果：通过；确认本地 dry-run 写出 `state_updated`、`search_graph`、`active_frontier`，且边界命中样本生成 root fork，探索中样本继续当前分支，无效分支生成 root fork。
6. `bash -n run_loop.sh`
   - 结果：未运行成功；当前 PowerShell 环境无法找到 `bash` 命令。已通过静态 pytest 覆盖 run loop 字符串级集成，但仍建议在 Bash-compatible shell 中补跑该命令。

### 未完成事项

- 未进行真实 LLM/API 端到端运行；本阶段验证使用 fixture、mock、CLI dry-run 和静态 run loop 检查。
- 未编写阶段06迁移说明、最终验收报告或真实 tiny experiment 复盘文档；这些属于下一阶段范围。
- 未迁移旧实验输出目录；阶段05仅支持在新轮次中写出 graph/frontier artifacts。

### 风险与注意事项

- `ENABLE_TREE_SEARCH` 默认仍为 `false`，用于保护旧单链流程；需要显式设置为 `true` 才会让 run loop 消费 frontier。
- 当前环境缺少 `bash`，因此 `run_loop.sh` 的真实 shell 语法检查需要在装有 Bash 的环境中补验。
- root fork 的 source prompt 优先依赖 `meta_info.prompt_old`；若历史记录缺失原始 prompt，会按兼容策略使用当前 prompt。
- 当前工作区在本阶段开始前已有阶段01-04及其他无关未提交变更，本阶段未回退或清理这些既有状态。

### 是否满足当前阶段完成标准

- 是。状态更新阶段已经集中维护预算、分支状态、已发现边界和无新增边界计数；命中边界、无效分支、深层终止分支均可生成下一轮 root/parent frontier；每轮可写出可复盘的 `search_graph.jsonl` 和 `active_frontier.jsonl`；run loop 已具备可回退配置与 frontier 消费入口；旧 `update_records()` 契约保持兼容。

### 是否建议进入下一阶段

- 是。建议下一次 Goal 进入阶段06。

### 下一次 Goal 自动应执行的阶段文档名称

- `阶段06_测试验证迁移兼容与实验验收.md`

## 2026-06-29 22:53:19 +08:00

### 自动选中的阶段

- 阶段编号：06
- 阶段文档：`阶段06_测试验证迁移兼容与实验验收.md`

### 本阶段完成内容

1. 复核任务规划文件夹、阶段总览和阶段执行记录，确认阶段01到阶段05均已明确满足完成标准并建议进入下一阶段，因此本次自动选择阶段06。
2. 扩展阶段06 tiny mock pipeline 验收：保留禁用树状搜索时的旧单链闭环验证，同时新增启用树状搜索的 mock 验收路径。
3. 新增启用树状搜索后的 artifact 断言，确认 tiny pipeline 可写出 `search_graph.jsonl` 与 `active_frontier.jsonl`，并可复盘 root/node/parent、边界轴、边界命中、边界叶子、去重签名、root fork 和剩余预算。
4. 新增阶段06验收报告与迁移兼容说明，记录默认参数、启用方式、旧数据兼容策略、验证覆盖、已知风险和后续调参建议。
5. 更新 schema 说明，将字段契约说明从 Stage 0-5 更新为 Stage 0-6，并明确阶段06不引入新运行时字段，只负责验证与验收文档。
6. 使用 fixture 与本地 CLI dry-run 验证阶段05 artifact 写出路径，确认 `state_updated`、`search_graph` 和 `active_frontier` 均可生成。

### 修改文件清单

- `tests/test_stage06_tiny_pipeline_artifacts.py`
- `schemas/README.md`
- `树状搜索与回溯式多边界探索_阶段规划/阶段执行记录.md`

### 新增文件清单

- `docs/树状搜索阶段06验收报告.md`

### 删除文件清单

- 无本阶段删除文件。

### 测试与验证结果

1. `python -m pytest tests/test_stage06_tiny_pipeline_artifacts.py -q`
   - 结果：通过，2 passed。
2. `python -m pytest tests/test_stage06_tiny_pipeline_artifacts.py tests/test_stage06_run_loop_integration.py -q`
   - 结果：通过，8 passed。
3. `python -m pytest`
   - 结果：通过，57 passed。
4. `python -m py_compile update_sample_state.py analyze_evolution_effect.py candidate_selection.py question_evolution.py profile_samples.py select_evolution_candidates.py operator_router.py`
   - 结果：通过。
5. `python analyze_evolution_effect.py --before tests/fixtures/stage05_previous_scored.jsonl --input tests/fixtures/stage05_scored.jsonl --output $TEMP/qe_stage06_effect_analysis.jsonl --matrix-output $TEMP/qe_stage06_effect_matrix.jsonl`
   - 结果：通过，成功写出 effect analysis 与 effect matrix 临时产物。
6. `python update_sample_state.py --input $TEMP/qe_stage06_effect_analysis.jsonl --output $TEMP/qe_stage06_state_updated.jsonl --tree-search-enabled true --active-frontier-output $TEMP/qe_stage06_active_frontier.jsonl --search-graph-output $TEMP/qe_stage06_search_graph.jsonl --no-memory-output`
   - 结果：通过，成功写出 `state_updated`、`active_frontier`、`search_graph`。
   - 产物检查：`state_rows=4`、`frontier_rows=4`、`graph_rows=4`；首条 graph 为 `branch_status=boundary_hit`、`is_boundary_hit=True`、`boundary_axis=最关键缺口识别`；首条 frontier 为 `action_type=fork_from_root`、`source_node_type=root`、`target_boundary_axis=结论分层`。
7. `Get-Command bash -ErrorAction SilentlyContinue`
   - 结果：当前 PowerShell 环境未找到 `bash`，因此未运行 `bash -n run_loop.sh`；已在验收报告中标记需在 Bash-compatible shell 中补验。

### 未完成事项

- 未进行真实 LLM/API 端到端运行；本阶段按规划优先使用 mock、fixture、pytest 和 CLI dry-run 验证结构正确性。
- 未迁移旧实验输出目录；阶段06仅说明兼容策略，新 artifacts 由后续新轮次生成。
- 未在当前环境执行 `bash -n run_loop.sh`，原因是当前 PowerShell 环境没有可用 `bash` 命令。

### 风险与注意事项

- `ENABLE_TREE_SEARCH` 默认仍为 `false`，正式启用树状搜索时必须显式设置为 `true`。
- tiny mock pipeline 证明结构闭环和 artifact 写出可用，但不代表真实模型输出稳定性或成本分布。
- 真实 LLM 输出如果出现新的能力轴表述，后续可能需要补充能力轴归一化关键词或 prompt 约束。
- 当前工作区在本阶段开始前已有阶段01-05及其他无关未提交/未跟踪变更，本阶段未回退或清理这些既有状态。

### 是否满足当前阶段完成标准

- 是。核心结构逻辑已有 focused pytest 覆盖；禁用树状搜索时旧 mock pipeline 可运行；启用树状搜索时 tiny mock pipeline 可写出并验证 `search_graph.jsonl` 与 `active_frontier.jsonl`；验收文档已说明已知风险、默认参数、运行方式和后续调参建议。

### 是否建议进入下一阶段

- 否。当前规划只包含阶段01到阶段06，阶段06已是本轮规划的最终验收阶段；后续如果继续，应由人工确认新的真实实验、调参或产品化规划。

### 下一次 Goal 自动应执行的阶段文档名称

- 无。阶段01到阶段06已全部完成，规划文件夹中不存在阶段07文档。
